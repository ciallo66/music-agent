# 前端基础业务接口闭环

本阶段补齐前端已调用但后端尚未实现的用户级接口：歌单与收藏。

- 所有接口必须经过 Access Token 鉴权，并按当前用户隔离数据。
- 歌单支持列表、详情、创建、修改、删除以及歌曲增删。
- 收藏使用 `(user_id, song_id)` 唯一约束，重复收藏返回冲突。
- 请求中引用不存在的歌曲或歌单返回 404；访问其他用户的歌单同样返回 404。
- routes 仅处理 HTTP 映射，业务规则放在 service，数据库访问放在 repository。
